% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities-defaults.R
\name{scale_colour_osp}
\alias{scale_colour_osp}
\alias{scale_color_osp}
\alias{scale_fill_osp}
\title{OSP discrete color and fill scales}
\usage{
scale_colour_osp(...)

scale_color_osp(...)

scale_fill_osp(...)
}
\arguments{
\item{...}{further arguments passed on to \code{\link[ggplot2:discrete_scale]{ggplot2::discrete_scale()}}.}
}
\value{
a discrete \code{ggplot2} scale.
}
\description{
Discrete \code{ggplot2} scales that apply the OSPSuite color palette per plot,
without mutating global \code{ggplot2} state. Add them to a plot with
\code{plot + scale_colour_osp()} or \code{plot + scale_fill_osp()}. All
\code{ospsuite.plots} plot functions apply these automatically (unless a color
or fill scale is already present), so explicit use is only needed to style
unrelated plots or to override a different scale.

The palette reproduces the previous global behavior of \code{\link[=setDefaults]{setDefaults()}}:
\code{colorMaps$default} (6 colors) is used when there are at most 6 groups,
otherwise \code{colorMaps$ospDefault} (50 colors).
}
\examples{
library(ggplot2)
df <- data.frame(x = 1:3, y = 1:3, group = c("A", "B", "C"))
ggplot(df, aes(x, y, color = group)) +
  geom_point() +
  scale_colour_osp()

}
\concept{scales}
