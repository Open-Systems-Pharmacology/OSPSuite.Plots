# create default labels with unit for plot

create default labels with unit for plot

## Usage

``` r
createDefaultPlotLabels(mappedData)
```

## Arguments

- mappedData:

  `MappedData` object with information of mapped dimensions and units

## Value

list with plot labels
