# Compute area-based scale factor for a shape

Compute area-based scale factor for a shape

## Usage

``` r
.computeAreaScale(spec, circleArea = NULL)
```

## Arguments

- spec:

  shape specification from .ospShapeSpec

- circleArea:

  area of reference circle (default: 64-gon approximation)

## Value

scale factor to match circle area
