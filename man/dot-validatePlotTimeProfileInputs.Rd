% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotTimeProfile.R
\name{.validatePlotTimeProfileInputs}
\alias{.validatePlotTimeProfileInputs}
\title{Validates input for \code{plotTimeProfile} function}
\usage{
.validatePlotTimeProfileInputs(
  data,
  observedData,
  plotObject,
  metaData,
  xscale,
  xscale.args,
  yscale,
  yscale.args,
  y2scale,
  y2scale.args,
  geomLineAttributes,
  geomRibbonAttributes,
  geomPointAttributes,
  geomErrorbarAttributes,
  geomLLOQAttributes,
  groupAesthetics,
  mapSimulatedAndObserved
)
}
\arguments{
\item{data}{data.frame with simulated data will be displayed as lines with ribbons}

\item{observedData}{data.frame with observed data  will be displayed as points with error-bars}

\item{plotObject}{An optional \code{ggplot} object on which to add the plot layers}

\item{metaData}{A named list of information about \code{data} such as the \code{dimension} and \code{unit} of its variables.}

\item{xscale}{either 'linear' then \code{ggplot2::scale_x_continuous()} or 'log' then \code{ggplot2::scale_x_log10()} is used}

\item{xscale.args}{list of arguments passed to \code{ggplot2::scale_x_continuous()} or \code{ggplot2::scale_x_log10()}}

\item{yscale}{either 'linear' then \code{ggplot2::scale_y_continuous()} or 'log' then \code{ggplot2::scale_y_log10()} is used}

\item{yscale.args}{list of arguments passed to \code{ggplot2::scale_y_continuous()} or \code{ggplot2::scale_y_log10()}}

\item{y2scale}{either 'linear' the secondary axis is displayed linear, or 'log' secondary axis is displayed with log scale}

\item{y2scale.args}{list of arguments passed to \code{ggplot2::sec_axis()}, trans, break are set by code}

\item{geomLineAttributes}{A \code{list} with arguments which are passed on to the call \code{ggplot2::geom_line}}

\item{geomRibbonAttributes}{A \code{list} with arguments which are passed on to the call \code{ggplot2::geom_ribbon}}

\item{geomPointAttributes}{A \code{list} with arguments which are passed on to the call \code{ggplot2::geom_point}}

\item{geomErrorbarAttributes}{A \code{list} with arguments which are passed on to the call \code{ggplot2::geom_errorbar}}

\item{geomLLOQAttributes}{A \code{list} with arguments which are passed on to the call \code{ggplot2::geom_hline}}

\item{groupAesthetics}{vector of aesthetics, which are used for columns mapped with \code{groupby},}

\item{mapSimulatedAndObserved}{table with columns observed and simulated which maps simulated and observed data
use of \code{mapSimulatedAndObserved} triggers reset of aesthetic scales after simulation layers}
}
\description{
Validates input for \code{plotTimeProfile} function
}
\keyword{internal}
