# Build shape specs with computed area-based scaling

Build shape specs with computed area-based scaling

## Usage

``` r
.buildOspShapeSpec()
```

## Value

list of shape specifications with scale factors
